[build]
  base    = "careers"
  command = "npm run build"
  publish = "dist/careers"

# Angular SPA — all routes serve index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
